version: "3.9"
services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      spring_profiles_active: "integration"
    volumes:
      - "./:/usr/src/myapp"
      - "./.m2:/root/.m2"
    command: >
      sh -c "pwd && ./mvnw test"
